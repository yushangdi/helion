name: Benchmark Dispatch

on:
  workflow_dispatch:
    inputs:
      run_h100:
        description: 'Run benchmark on H100'
        required: false
        type: boolean
        default: false
      run_b200:
        description: 'Run benchmark on B200'
        required: false
        type: boolean
        default: true
      run_mi350x:
        description: 'Run benchmark on MI350X'
        required: false
        type: boolean
        default: false
      run_tpu:
        description: 'Run benchmark on TPU v7'
        required: false
        type: boolean
        default: false
      run_b200_cute:
        description: 'Run benchmark on B200 with the Helion CuTe backend'
        required: false
        type: boolean
        default: false
      kernels:
        description: 'Comma-separated list of kernels to benchmark on GPU runners'
        required: false
        type: string
        # Ordered to balance B200 runtime under compute-benchmark-matrix's
        # round-robin sharding with max-runners=13.
        # rope-bwd temporarily removed: autotune trips Triton's
        # LinearLayout::reshapeOuts assertion on H100/B200/MI350X and aborts
        # the parent process, producing Shape Failures for every shape. Re-add
        # once #2531 (compile-phase subprocess isolation) lands or the upstream
        # Triton bug is fixed. Forward `rope` stays in the list.
        default: "flash_attention,gdn_fwd_h,cross_entropy,grouped_gemm,gemm,welford,layer_norm-bwd,int4_gemm,softmax,layer_norm,mamba2_chunk_state,rms_norm-bwd,rope,jsd,mamba2_chunk_scan,kl_div,rms_norm"
      kernels_tpu:
        description: 'Kernels benchmarked on TPU via run_tpu.py: only the kernels the tritonbench bridge cannot handle — bmm (no tritonbench operator) and cross_entropy (Helion Mosaic-codegen holdout). Everything else runs via the bridge (kernels_tpu_bridge). `all` runs every kernel in benchmarks/run_tpu.py::KERNEL_MAPPINGS.'
        required: false
        type: string
        default: "bmm,cross_entropy"
      kernels_tpu_bridge:
        description: 'Kernels benchmarked on TPU via the tritonbench bridge (benchmarks/run.py --device tpu). These land in the shared `tpu` dashboard column alongside kernels_tpu (disjoint sets).'
        required: false
        type: string
        default: "flash_attention,gemm,welford,softmax,layer_norm,rms_norm,rms_norm-bwd,kl_div"
      kernels_cute:
        description: 'Comma-separated list of kernels to benchmark on B200 with HELION_BACKEND=cute. Defaults to the CuTe-capable TritonBench subset.'
        required: false
        type: string
        default: "vector_add,layer_norm,rms_norm,vector_exp,sum"
      env_vars:
        description: 'Environment variables for benchmark runner'
        required: false
        type: string
        default: ""
      custom_args:
        description: 'Custom arguments to append to benchmark commands'
        required: false
        type: string
        default: ""

jobs:
  gen-matrix-h100:
    if: ${{ github.event.inputs.run_h100 == 'true' }}
    uses: ./.github/workflows/compute-benchmark-matrix.yml
    with:
      max-runners: 13
      kernels: ${{ github.event.inputs.kernels }}

  run-h100:
    needs: gen-matrix-h100
    uses: ./.github/workflows/benchmark.yml
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.gen-matrix-h100.outputs.matrix) }}
    permissions:
      id-token: write
      contents: read
    with:
      runner: mt-l-x86iamx-22-225-h100
      python-version: "3.12"
      image: nvidia/cuda:13.1.0-devel-ubuntu24.04
      runtime-version: cu130
      container-options: --gpus all
      alias: h100
      kernels: ${{ matrix.kernels }}
      env-vars: ${{ github.event.inputs.env_vars }}
      custom-args: ${{ github.event.inputs.custom_args }}

  gen-matrix-b200:
    uses: ./.github/workflows/compute-benchmark-matrix.yml
    if: ${{ github.event.inputs.run_b200 == 'true' }}
    with:
      max-runners: 13
      kernels: ${{ github.event.inputs.kernels }}

  run-b200:
    needs: gen-matrix-b200
    uses: ./.github/workflows/benchmark.yml
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.gen-matrix-b200.outputs.matrix) }}
    permissions:
      id-token: write
      contents: read
    with:
      runner: linux.dgx.b200
      python-version: "3.12"
      image: nvidia/cuda:13.1.0-devel-ubuntu24.04
      runtime-version: cu130
      container-options: --gpus all
      alias: b200
      kernels: ${{ matrix.kernels }}
      env-vars: ${{ github.event.inputs.env_vars }}
      custom-args: ${{ github.event.inputs.custom_args }}

  gen-matrix-b200-cute:
    uses: ./.github/workflows/compute-benchmark-matrix.yml
    if: ${{ github.event.inputs.run_b200_cute == 'true' }}
    with:
      max-runners: 6
      kernels: ${{ github.event.inputs.kernels_cute }}

  run-b200-cute:
    needs: gen-matrix-b200-cute
    uses: ./.github/workflows/benchmark.yml
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.gen-matrix-b200-cute.outputs.matrix) }}
    permissions:
      id-token: write
      contents: read
    with:
      runner: linux.dgx.b200
      python-version: "3.12"
      image: nvidia/cuda:13.1.0-devel-ubuntu24.04
      runtime-version: cu130
      container-options: --gpus all
      alias: b200_cute
      backend: cute
      kernels: ${{ matrix.kernels }}
      env-vars: ${{ github.event.inputs.env_vars }}
      custom-args: ${{ github.event.inputs.custom_args }}

  gen-matrix-mi350x:
    uses: ./.github/workflows/compute-benchmark-matrix.yml
    if: ${{ github.event.inputs.run_mi350x == 'true' }}
    with:
      max-runners: 13
      kernels: ${{ github.event.inputs.kernels }}

  run-mi350x:
    needs: gen-matrix-mi350x
    uses: ./.github/workflows/benchmark.yml
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.gen-matrix-mi350x.outputs.matrix) }}
    permissions:
      id-token: write
      contents: read
    with:
      runner: linux.rocm.gpu.ecosystem.gfx950.1
      python-version: "3.12"
      image: rocm/dev-ubuntu-24.04:7.1.1-complete
      runtime-version: rocm7.1
      container-options: --device=/dev/kfd --device=/dev/dri
      alias: mi350x
      kernels: ${{ matrix.kernels }}
      env-vars: ${{ github.event.inputs.env_vars }}
      custom-args: ${{ github.event.inputs.custom_args }}

  run-tpu:
    if: ${{ github.event.inputs.run_tpu == 'true' }}
    uses: ./.github/workflows/benchmark_tpu.yml
    permissions:
      contents: read
    with:
      alias: tpu
      kernels: ${{ github.event.inputs.kernels_tpu }}
      env-vars: ${{ github.event.inputs.env_vars }}

  # Both TPU jobs (run_tpu.py for the holdouts, the bridge for everything else)
  # share the single `run_tpu` toggle; together they cover the `tpu` dashboard.
  run-tpu-bridge:
    if: ${{ github.event.inputs.run_tpu == 'true' }}
    uses: ./.github/workflows/benchmark_tpu_bridge.yml
    permissions:
      contents: read
    with:
      alias: tpu_bridge
      kernels: ${{ github.event.inputs.kernels_tpu_bridge }}
      env-vars: ${{ github.event.inputs.env_vars }}

  trigger-docs-deploy:
    if: always()
    needs: [run-b200, run-b200-cute, run-h100, run-mi350x, run-tpu, run-tpu-bridge]
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: Trigger docs deploy
        uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'docs-deploy.yml',
              ref: 'main'
            })
